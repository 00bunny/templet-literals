let firstname: string = "Talha"
let secondname: string= "Ahmed"

let fullname: string= firstname + '' + secondname;

console.log(fullname);

let firstName: string="Talha";
let secondName: string="Ahmed";

let fullName: string= `${firstName} ${secondName}`;

console.log(fullName);